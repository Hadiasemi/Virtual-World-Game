import processing.core.PImage;

import java.util.List;

public class White extends Entity {
    public White(Point position, List<PImage> images, int imageIndex)
    {
        super(position, images, imageIndex);
    }
}
