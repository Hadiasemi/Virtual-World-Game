import processing.core.PImage;

import java.util.List;

public class Green extends Entity{
    public Green(Point position, List<PImage> images, int imageIndex)
    {
        super(position, images, imageIndex);
    }
}
