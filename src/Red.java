import processing.core.PImage;
import java.util.List;

public class Red extends Entity {

    public Red(Point position, List<PImage> images, int imageIndex)
    {
        super(position, images, imageIndex);
    }

}
